# Default: production API (no local backend required)
# VITE_API_URL=https://api.vidyank.com

# Local backend (start backend first, then uncomment):
# VITE_API_URL=http://127.0.0.1:3000

# Or use Vite proxy (empty URL + backend on port 3000):
# VITE_API_URL=
